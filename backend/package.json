{
  "name": "string-backend",
  "version": "0.2.2",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply string-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply string-db --remote"
  },
  "dependencies": {
    "@x402/core": "^2.2.0",
    "@x402/evm": "^2.2.0",
    "hono": "^4.11.3",
    "poseidon-lite": "^0.3.0",
    "viem": "^2.44.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260111.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.58.0"
  }
}
